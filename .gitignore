# Build caches. Regenerated by analyze_memes.py; several MB each and keyed to
# the exact file list, so they are worthless to anyone else.
.cache_clip*.npz
.cache_ocr.json

# Backups taken before edits
*.bak

# Python
__pycache__/
*.py[cod]

# Node
node_modules/
.env
captures/
